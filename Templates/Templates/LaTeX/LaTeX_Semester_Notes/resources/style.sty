\RequirePackage{standalone}
% \RequirePackage{cancel}
\RequirePackage[utf8]{inputenc}
\RequirePackage[english]{babel}
\RequirePackage[colorlinks=true,linkcolor=blue,urlcolor=blue,bookmarksopen=true]{hyperref}
\RequirePackage[margin=1.5in]{geometry}
 \RequirePackage{centernot}
 \RequirePackage{wrapfig}
 \RequirePackage{xcolor}
 \RequirePackage{amsmath, amssymb, amsfonts}
\RequirePackage{svg}
 \RequirePackage{esint} % for making \oint
 \RequirePackage{multicol}
 \RequirePackage{steinmetz}
 \RequirePackage{lipsum}% http://ctan.org/pkg/lipsum
 \RequirePackage{lmodern, libertine}
 \RequirePackage{graphicx}
 \RequirePackage{xfrac}
\RequirePackage[depth=5]{bookmark}% Show up to level 4 (\paragraph) in bookmarks
 \RequirePackage{titlesec} %This is to stop the automatic numbering of sections (you can't use * with TOC)
\RequirePackage{tcolorbox} %Theorem Boxes
 \RequirePackage{fancyhdr}
\RequirePackage{tikz}
\RequirePackage{pstricks}


\usepackage{fancyhdr}
\pagestyle{fancy}
%\fancyhf{}
%\fancyhead[R]{\leftmark}
%\fancyhead[L]{\rightmark}
%\renewcommand\headrulewidth{0pt}
%\renewcommand\chaptermark[1]{\markboth{#1}{}}
%\renewcommand\sectionmark[1]{\markright{\thesection.\ #1}}
\rhead[\rightmark]{\rightmark} % SubSection
\lhead[\leftmark]{\leftmark} % Section


\AtBeginDocument{\maketitle}
\AtBeginDocument{\tableofcontents}



%
%
%
%% %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% %% Load SubFiles Last %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%
%% The difference between ~subfiles~ and ~standalone~ is that ~subfiles~ uses the
%% preamble referenced in the main ~tex~ file whereas ~standalone~ can have a
%% seperate file for each.
%%
%% The ~subfiles~ might be easier if the latex document should generally stay
%% combined, but it isn't supported well by pandoc and might be a pain in the ass
%% if I want to take out just one file.
%
\RequirePackage{import} % apparently this is safer than \input{} https://www.overleaf.com/learn/latex/Multi-file_LaTeX_projects
